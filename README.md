# WithDay

여행 동행 매칭 플랫폼

## 프로젝트 개요

WithDay는 사용자 성향과 관심사를 기반으로 여행 동행을 찾고, 추천 일정을 활용해 여행 계획을 등록할 수 있는 웹 플랫폼입니다.

## 프로젝트 기간

2026.05.11 ~ 2026.06.09

## 팀 구성

4인 팀 프로젝트

## 이영민 담당 역할

* 회원가입
* 로그인 / 로그아웃
* Google 소셜 로그인
* 아이디 찾기
* 비밀번호 찾기
* 소셜 계정 비밀번호 재설정 제한
* 추천 일정 목록 / 상세 조회
* 추천 일정 사용하기
* 추천 일정 등록 / 수정 / 삭제
* 추천 일정 이미지 처리
* 관리자 약관 관리
* 관리자 관심사 관리
* AWS CloudFront 기반 별도 배포 경험

## 기술 스택

* Frontend: React, Vite, JavaScript, HTML, CSS
* Backend: Java, Spring Boot, MyBatis, REST API, Gradle
* Database: MySQL
* Auth / Security: Spring Security, JWT, BCrypt, Google OAuth
* API / Integration: Cloudinary, OneSignal, Spring Boot Mail, Email Verification
* Deploy / Cloud: AWS, CloudFront, Docker, GitHub Actions

## 주요 구현 내용

### 1. 회원가입 / 로그인 / 인증

* 이메일, 비밀번호, 닉네임, 생년월일, 성별, 연락처, 주소 등 가입 정보 입력
* 이메일 인증번호 발송 및 검증
* DB 약관 목록 조회 및 필수 약관 동의 검증
* 관심사 목록 조회 및 다중 선택
* 이메일 중복, 비밀번호 형식, 만 18세 이상 검증
* 프로필 이미지 업로드 및 Cloudinary 저장 URL 반영
* 비밀번호 BCrypt 암호화 저장
* JWT 기반 로그인 상태 관리
* 자동 로그인 선택에 따른 JWT 유지 방식 분기
* 로그아웃 시 JWT와 사용자 정보 초기화

### 2. Google 소셜 로그인

* Google OAuth 기반 로그인 처리
* 기존 구글 회원 여부 확인
* 미가입자는 추가 정보 입력 화면으로 이동
* 추가 정보, 약관 동의, 관심사 저장 후 소셜 회원가입 완료
* provider 기준으로 일반 계정과 소셜 계정 인증 흐름 분리

### 3. 아이디 / 비밀번호 찾기

* 닉네임 / 전화번호 기반 아이디 찾기
* 이메일 기반 비밀번호 찾기
* 소셜 계정은 일반 비밀번호 재설정을 사용할 수 없도록 제한
* 이메일 인증 후 새 비밀번호 검증 및 BCrypt 저장

### 4. 추천 일정

* 추천 일정 목록 조회
* 카테고리 필터
* 추천 일정 상세 조회
* 추천 일정 사용하기 기능
* 추천 일정 데이터를 일반 일정 글쓰기 화면의 기본값으로 전달
* 실제 일정 기간, 모집 기간, 상세 일정 등은 사용자가 직접 입력하도록 분리
* 추천 일정 등록 / 수정 / 삭제
* 추천 일정 이미지 업로드, 유지, 삭제 상태 관리

### 5. 관리자 약관 / 관심사 관리

* 약관 목록 조회
* 약관 버전 / 내용 수정
* 관심사 목록 조회
* 관심사 추가 / 수정 / 삭제
* 회원가입 및 마이페이지 관심사 선택 흐름과 연결

### 6. 별도 배포 경험

* 팀 배포와 별도로 AWS CloudFront 주소를 기준으로 직접 배포를 진행
* 배포 환경에서 화면 접근, API 연결, 정적 리소스 로딩 여부를 점검
* CloudFront 기반 배포 흐름과 환경변수 / API 연결 점검 과정을 경험

대표 배포 주소:
[https://d3276jh72yu5am.cloudfront.net/](https://d3276jh72yu5am.cloudfront.net/)

## 트러블슈팅

### 1. 소셜 회원가입 접근 오류

* 문제: Google 로그인 후 추가 정보 입력이 필요한 사용자가 일반 회원가입 흐름과 섞이며 접근 흐름이 불안정했습니다.
* 원인: 소셜 로그인 응답 이후 필요한 약관 동의, 관심사 선택, 추가 정보 저장 단계가 명확히 분리되지 않았습니다.
* 해결: 소셜 가입 전용 추가 정보 화면을 분리하고 OAuth 응답 데이터, 약관 동의, 관심사 선택값을 검증한 뒤 저장하도록 흐름을 정리했습니다.
* 결과: Google 계정 사용자가 누락 정보 없이 소셜 회원가입을 완료하고 로그인 상태로 서비스에 진입할 수 있게 되었습니다.
* 배운 점: 외부 인증은 로그인 성공만으로 끝나지 않고, 서비스 내부 회원 정보 완성 단계까지 하나의 가입 플로우로 설계해야 한다는 점을 배웠습니다.

### 2. 소셜 계정 재설정 제한

* 문제: Google로 가입한 사용자가 로컬 계정용 비밀번호 찾기 / 재설정 기능에 접근할 수 있는 문제가 있었습니다.
* 원인: 로컬 계정과 소셜 계정의 인증 수단 차이를 화면과 API 요청 단계에서 충분히 구분하지 않았습니다.
* 해결: 계정 provider가 local이 아닌 경우 비밀번호 재설정 진입을 제한하고, 사용자에게 소셜 로그인 계정임을 안내하도록 처리했습니다.
* 결과: 비밀번호가 없는 소셜 계정에 재설정 요청이 발생하지 않아 인증 흐름이 명확해졌습니다.
* 배운 점: 계정 유형별로 가능한 동작을 명확히 제한해야 보안과 사용자 안내가 함께 안정화된다는 점을 익혔습니다.

### 3. 추천 일정 초기값 오류

* 문제: 추천 일정을 사용해 일반 일정 등록 화면으로 이동할 때 제목, 지역, 카테고리 등 일부 초기값이 바로 반영되지 않았습니다.
* 원인: 추천 일정 데이터는 먼저 전달됐지만 지역 / 카테고리 옵션 데이터가 늦게 준비되면서 선택값 매핑 순서가 어긋났습니다.
* 해결: 추천 일정 payload를 등록 화면용 값으로 정규화하고, 옵션 데이터 로딩 이후 선택값을 다시 동기화하도록 상태 반영 순서를 조정했습니다.
* 결과: 추천 일정 사용 시 필요한 기본 정보가 일정 등록 화면에 안정적으로 채워졌습니다.
* 배운 점: 비동기 데이터가 여러 개 섞이는 화면에서는 초기값 주입 시점과 옵션 데이터 준비 시점을 함께 설계해야 한다는 점을 확인했습니다.

### 4. 추천 일정 이미지 삭제 오류

* 문제: 추천 일정 수정 중 기존 이미지를 삭제해도 저장 후 다시 보이거나, 새 이미지와 삭제 이미지 상태가 섞이는 문제가 있었습니다.
* 원인: 기존 이미지 URL, 새로 추가한 파일, 삭제 대상 이미지가 하나의 화면 상태에서 명확히 분리되지 않았습니다.
* 해결: 유지할 이미지, 삭제할 이미지, 새로 업로드할 이미지를 별도 상태로 관리하고 multipart 요청에 명시적으로 포함했습니다.
* 결과: 추천 일정 수정 시 이미지 추가 / 삭제 결과가 저장 후에도 일관되게 유지되었습니다.
* 배운 점: 파일 업로드 기능은 화면 미리보기 상태와 서버에 전달할 변경 명세를 분리해야 안정적으로 관리할 수 있음을 배웠습니다.

## 배포 링크

* Live Demo: [https://d3276jh72yu5am.cloudfront.net/](https://d3276jh72yu5am.cloudfront.net/)
* GitHub: [https://github.com/yglee0248-cyber/WithDay](https://github.com/yglee0248-cyber/WithDay)

## 실행 방법

프론트엔드와 백엔드는 각각 `WithDayFront`, `WithDayBack` 폴더로 분리되어 있습니다.

### Backend

```bash
cd WithDayBack
./gradlew bootRun
```

Windows PowerShell에서는 아래 명령어를 사용할 수 있습니다.

```powershell
cd WithDayBack
.\gradlew.bat bootRun
```

백엔드 기본 포트는 `10400`입니다. 실행 전 DB, 메일, Cloudinary, OneSignal 관련 환경변수 또는 프로퍼티 설정이 필요합니다.

### Frontend

```bash
cd WithDayFront
npm install
npm run dev
```

프론트엔드는 `VITE_BACKSERVER` 환경변수로 백엔드 API 주소를 참조합니다.

## 저장소 구조

```text
WithDay/
├── WithDayBack/   # Spring Boot 백엔드
└── WithDayFront/  # React + Vite 프론트엔드
```
