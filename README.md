# WithDay

> 여행 동행 매칭 플랫폼

## 프로젝트 소개

WithDay는 국비지원 과정에서 팀 프로젝트로 진행한 여행 동행 매칭 플랫폼입니다.

사용자는 여행 또는 일정을 등록할 수 있고, 다른 사용자는 해당 일정에 동행을 신청할 수 있습니다.
또한 관심사 기반 추천 일정, Google 소셜 로그인, 이메일 인증, 이미지 업로드, 알림 기능 등을 통해 여행 동행 서비스를 구성했습니다.

## 주요 기능

* **회원 인증**: 일반 회원가입, 로그인, 로그아웃, Google 소셜 로그인, 이메일 인증, 아이디 / 비밀번호 찾기 기능을 제공합니다.
* **일정 탐색 및 관리**: 사용자는 여행 또는 일정을 등록하고, 일정 목록과 상세 정보를 조회할 수 있습니다.
* **동행 신청**: 사용자는 일정에 동행을 신청할 수 있고, 일정 작성자는 신청을 승인하거나 거절할 수 있습니다.
* **추천 일정**: 관리자가 등록한 추천 일정을 확인하고, 일반 일정 작성 시 기본 정보로 활용할 수 있습니다.
* **위시리스트**: 관심 있는 일정을 저장하고 다시 확인할 수 있습니다.
* **알림 기능**: 동행 신청, 승인 등 주요 이벤트에 대한 알림을 제공합니다.
* **관리자 기능**: 약관, 관심사, 추천 일정 등 서비스 관리에 필요한 일부 데이터를 관리할 수 있습니다.
* **이미지 업로드**: 프로필 이미지와 일정 이미지를 업로드하고 저장된 이미지 URL을 서비스 화면에 반영합니다.

## 프로젝트 정보

| 항목    | 내용                                                     |
| ----- | ------------------------------------------------------ |
| 프로젝트명 | WithDay                                                |
| 주제    | 여행 동행 매칭 플랫폼                                           |
| 기간    | 2026.05.11 ~ 2026.06.09                                |
| 형태    | 국비지원 과정 파이널 팀 프로젝트                                     |
| 팀 구성  | 4인 팀 프로젝트                                              |
| 담당 영역 | 회원 인증, Google 소셜 로그인, 추천 일정, 관리자 약관 / 관심사 관리, 별도 배포 경험 |

## 기술 스택

### Frontend

* React 19
* Vite 8
* JavaScript
* React Router DOM 7
* Zustand 5
* TanStack Query / React Query 5
* React Hook Form 7
* MUI 9 / Emotion 11
* Axios 1

### Backend

* Java 17
* Spring Boot 4.0.6
* Spring MVC
* Spring Security
* MyBatis 4.0.1
* REST API
* Gradle

### Database

* MySQL

### Auth / Security

* JWT
* BCrypt
* Google OAuth

### API / Integration

* Cloudinary
* OneSignal
* Spring Boot Mail
* Email Verification

### Deploy / Infra

* AWS
* S3
* CloudFront
* EC2
* Docker
* Docker Compose
* GitHub Actions

## 프로젝트 구조

```text
WithDay/
 ├── WithDayFront/               # 프론트엔드 프로젝트 루트
 │    ├── src/app/               # 전역 라우팅 및 쿼리 클라이언트 설정
 │    ├── src/features/          # 도메인별 기능 컴포넌트 및 로직
 │    ├── src/page/              # 페이지 단위 컴포넌트
 │    ├── src/shared/            # 공통 API, 유틸리티, UI 컴포넌트
 │    └── src/widgets/           # 공통 레이아웃 및 위젯 컴포넌트
 ├── WithDayBack/                # 백엔드 프로젝트 루트
 │    ├── src/main/java/         # API, Service, Domain 로직
 │    ├── src/main/resources/    # 설정 파일 및 MyBatis Mapper XML
 │    ├── Dockerfile             # 백엔드 Docker 이미지 빌드 설정
 │    └── docker-compose.yml     # 백엔드 배포용 컨테이너 실행 설정
 └── .github/workflows/          # 프론트엔드 / 백엔드 배포 워크플로우
```

## 실행 방법

프론트엔드와 백엔드는 각각 `WithDayFront`, `WithDayBack` 폴더로 분리되어 있습니다.

### Frontend

```bash
cd WithDayFront
npm install
npm run dev
npm run build
```

### Backend

```bash
cd WithDayBack
./gradlew build
./gradlew bootRun
```

Windows 환경에서는 아래 명령어를 사용할 수 있습니다.

```bash
cd WithDayBack
gradlew.bat build
gradlew.bat bootRun
```

백엔드 기본 포트는 `10400`입니다.

## 환경 변수

로컬 실행 시 필요한 환경변수를 설정해야 합니다.
실제 값은 저장소에 포함하지 않습니다.

### Frontend

```bash
VITE_ONESIGNAL_APP_ID=
VITE_GOOGLE_CLIENT_ID=
VITE_BACKSERVER=
```

### Backend

```bash
DB_URL=
DB_USERNAME=
DB_PASSWORD=
MAIL_USERNAME=
MAIL_PASSWORD=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
ONESIGNAL_APP_ID=
ONESIGNAL_API_KEY=
```

필요한 경우 `SERVER_PORT`로 백엔드 실행 포트를 변경할 수 있습니다.

환경변수 관리 시 아래 내용을 지켜야 합니다.

* `.env`, `application-local.properties` 등 실제 환경변수 파일은 Git에 포함하지 않습니다.
* API Key, Secret, DB 접속정보는 공개 저장소에 올리지 않습니다.
* 실제 값이 노출된 적이 있다면 해당 키는 재발급 또는 폐기해야 합니다.

## 배포 안내

* Live Demo: https://d3276jh72yu5am.cloudfront.net/

> 위 배포 주소는 학습 및 포트폴리오 확인 목적으로 별도 구성한 배포 환경입니다.
> AWS 비용 또는 환경 정리 상황에 따라 추후 접속이 중단될 수 있습니다.

## 이영민 담당 역할

* 회원가입
* 로그인 / 로그아웃
* Google 소셜 로그인
* 아이디 찾기
* 비밀번호 찾기
* 소셜 계정 비밀번호 재설정 제한
* 추천 일정 목록 / 상세 조회
* 추천 일정 사용하기
* 추천 일정 등록 / 수정 / 삭제
* 추천 일정 이미지 처리
* 관리자 약관 관리
* 관리자 관심사 관리
* AWS 기반 별도 배포 및 연결 점검

## 이영민 주요 구현 내용

### 1. 회원가입 / 로그인 / 인증

* 이메일, 비밀번호, 닉네임, 생년월일, 성별, 연락처, 주소 등 가입 정보 입력 흐름 구현
* 이메일 인증번호 발송 및 검증 흐름 구현
* DB 약관 목록 조회 및 필수 약관 동의 검증
* 관심사 목록 조회 및 다중 선택 처리
* 이메일 중복, 비밀번호 형식, 만 18세 이상 검증
* 프로필 이미지 업로드 및 Cloudinary 저장 URL 반영
* 비밀번호 BCrypt 암호화 저장
* JWT 기반 로그인 상태 관리
* 자동 로그인 선택에 따른 JWT 유지 방식 분기
* 로그아웃 시 JWT와 사용자 정보 초기화

### 2. Google 소셜 로그인

* Google OAuth 기반 로그인 처리
* 기존 구글 회원 여부 확인
* 미가입자는 추가 정보 입력 화면으로 이동
* 추가 정보, 약관 동의, 관심사 저장 후 소셜 회원가입 완료
* provider 기준으로 일반 계정과 소셜 계정 인증 흐름 분리

### 3. 아이디 / 비밀번호 찾기

* 닉네임 / 전화번호 기반 아이디 찾기
* 이메일 기반 비밀번호 찾기
* 소셜 계정은 일반 비밀번호 재설정을 사용할 수 없도록 제한
* 이메일 인증 후 새 비밀번호 검증 및 BCrypt 저장

### 4. 추천 일정

* 추천 일정 목록 조회
* 카테고리 필터
* 추천 일정 상세 조회
* 추천 일정 사용하기 기능
* 추천 일정 데이터를 일반 일정 글쓰기 화면의 기본값으로 전달
* 실제 일정 기간, 모집 기간, 상세 일정 등은 사용자가 직접 입력하도록 분리
* 추천 일정 등록 / 수정 / 삭제
* 추천 일정 이미지 업로드, 유지, 삭제 상태 관리

### 5. 관리자 약관 / 관심사 관리

* 약관 목록 조회
* 약관 버전 / 내용 수정
* 관심사 목록 조회
* 관심사 추가 / 수정 / 삭제
* 회원가입 및 마이페이지 관심사 선택 흐름과 연결

### 6. 별도 배포 및 연결 점검

* 팀 배포와 별도로 AWS 기반 배포 흐름을 직접 구성하고 확인
* 배포 환경에서 화면 접근, API 연결, 정적 리소스 로딩 여부 점검
* CloudFront 기반 정적 리소스 배포 흐름과 환경변수 / API 연결 점검 과정 경험
* 해당 배포는 학습 및 포트폴리오 확인 목적의 별도 배포이며, 비용 또는 환경 문제에 따라 중단될 수 있음

## 트러블슈팅

### 1. 소셜 회원가입 접근 오류

* **문제 상황**: Google 로그인 후 추가 정보 입력이 필요한 사용자가 일반 회원가입 흐름과 섞이며 접근 흐름이 불안정했습니다.
* **원인**: 소셜 로그인 응답 이후 필요한 약관 동의, 관심사 선택, 추가 정보 저장 단계가 명확히 분리되지 않았습니다.
* **해결 방법**: 소셜 가입 전용 추가 정보 화면을 분리하고 OAuth 응답 데이터, 약관 동의, 관심사 선택값을 검증한 뒤 저장하도록 흐름을 정리했습니다.
* **결과**: Google 계정 사용자가 누락 정보 없이 소셜 회원가입을 완료하고 로그인 상태로 서비스에 진입할 수 있게 되었습니다.
* **배운 점**: 외부 인증은 로그인 성공만으로 끝나지 않고, 서비스 내부 회원 정보 완성 단계까지 하나의 가입 플로우로 설계해야 한다는 점을 배웠습니다.

### 2. 소셜 계정 재설정 제한

* **문제 상황**: Google로 가입한 사용자가 로컬 계정용 비밀번호 찾기 / 재설정 기능에 접근할 수 있는 문제가 있었습니다.
* **원인**: 로컬 계정과 소셜 계정의 인증 수단 차이를 화면과 API 요청 단계에서 충분히 구분하지 않았습니다.
* **해결 방법**: 계정 provider가 local이 아닌 경우 비밀번호 재설정 진입을 제한하고, 사용자에게 소셜 로그인 계정임을 안내하도록 처리했습니다.
* **결과**: 비밀번호가 없는 소셜 계정에 재설정 요청이 발생하지 않아 인증 흐름이 명확해졌습니다.
* **배운 점**: 계정 유형별로 가능한 동작을 명확히 제한해야 보안과 사용자 안내가 함께 안정화된다는 점을 익혔습니다.

### 3. 추천 일정 초기값 오류

* **문제 상황**: 추천 일정을 사용해 일반 일정 등록 화면으로 이동할 때 제목, 지역, 카테고리 등 일부 초기값이 바로 반영되지 않았습니다.
* **원인**: 추천 일정 데이터는 먼저 전달됐지만 지역 / 카테고리 옵션 데이터가 늦게 준비되면서 선택값 매핑 순서가 어긋났습니다.
* **해결 방법**: 추천 일정 payload를 등록 화면용 값으로 정규화하고, 옵션 데이터 로딩 이후 선택값을 다시 동기화하도록 상태 반영 순서를 조정했습니다.
* **결과**: 추천 일정 사용 시 필요한 기본 정보가 일정 등록 화면에 안정적으로 채워졌습니다.
* **배운 점**: 비동기 데이터가 여러 개 섞이는 화면에서는 초기값 주입 시점과 옵션 데이터 준비 시점을 함께 설계해야 한다는 점을 확인했습니다.

### 4. 추천 일정 이미지 삭제 오류

* **문제 상황**: 추천 일정 수정 중 기존 이미지를 삭제해도 저장 후 다시 보이거나, 새 이미지와 삭제 이미지 상태가 섞이는 문제가 있었습니다.
* **원인**: 기존 이미지 URL, 새로 추가한 파일, 삭제 대상 이미지가 하나의 화면 상태에서 명확히 분리되지 않았습니다.
* **해결 방법**: 유지할 이미지, 삭제할 이미지, 새로 업로드할 이미지를 별도 상태로 관리하고 multipart 요청에 명시적으로 포함했습니다.
* **결과**: 추천 일정 수정 시 이미지 추가 / 삭제 결과가 저장 후에도 일관되게 유지되었습니다.
* **배운 점**: 파일 업로드 기능은 화면 미리보기 상태와 서버에 전달할 변경 명세를 분리해야 안정적으로 관리할 수 있음을 배웠습니다.

## 프로젝트 한 줄 요약

WithDay는 여행 일정 등록, 동행 신청, 추천 일정, 인증, 알림, 관리자 기능을 포함한 여행 동행 매칭 플랫폼입니다.
이영민은 회원 인증, Google 소셜 로그인, 추천 일정, 관리자 약관 / 관심사 관리, 별도 배포 및 연결 점검을 중심으로 프로젝트에 참여했습니다.
