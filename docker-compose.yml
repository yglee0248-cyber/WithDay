services:
  backend:
    image: withday-backend:latest
    container_name: withday-backend
    restart: unless-stopped
    ports:
      - "8080:10400"
    env_file:
      - .env
    environment:
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx384m -XX:+UseContainerSupport"